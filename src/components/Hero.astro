<section class="hero">
  <div class="container">
    <div class="content">
      <h1 class="name">
        Gustavo<br />
        <span class="gradient-text">Garozzo</span>
      </h1>

      <p class="role">
        Desarrollo de Software & Backend Ssr — Node.js & NestJS
      </p>

      <p class="tagline">
        Especialista en desarrollo de software backend enfocado en robustez, performance y diseño de sistemas escalables y mantenibles.
      </p>

      <nav class="links">
        <div class="primary-actions">
          <a href="/cv.pdf" target="_blank" class="btn-primary">
            Descargar CV
          </a>
          <a href="/guia-nestjs.pdf" download class="btn-secondary">
            Guía NestJS Trainee
          </a>
        </div>
        <div class="social-links">
          <a href="mailto:gustavogarozzo@gmail.com" class="link-item">Email</a>
          <a href="https://github.com/gusgarozzo" target="_blank" class="link-item">GitHub</a>
          <a href="https://linkedin.com/in/gusgarozzo" target="_blank" class="link-item">LinkedIn</a>
        </div>
      </nav>
    </div>

    <div class="hero-graphic">
      <div class="terminal-window">
        <div class="terminal-header">
          <div class="dot red"></div>
          <div class="dot yellow"></div>
          <div class="dot green"></div>
        </div>
        <div class="terminal-body" id="console-content">
          <!-- Handled by script to avoid CLS -->
        </div>
      </div>
    </div>
  </div>
  
  <div class="decoration"></div>

  <style>
    .hero {
      padding: 4rem 0;
      position: relative;
      overflow: visible;
    }

    @media (max-width: 768px) {
      .hero {
        padding: 2rem 0;
      }
    }

    .container {
      display: grid;
      grid-template-columns: 1fr;
      gap: 3rem;
      align-items: center;
    }

    @media (min-width: 900px) {
      .container {
        grid-template-columns: 1.2fr 0.8fr;
      }
    }

    .name {
      font-size: 4rem;
      font-weight: 800;
      line-height: 1.1; /* Slightly more space */
      margin-bottom: 2rem;
      letter-spacing: -0.04em;
    }
    
    @media (min-width: 768px) {
      .name {
         font-size: 5.5rem; /* Larger name */
      }
    }

    .role {
      font-size: 1.25rem;
      color: #94a3b8;
      margin-bottom: 1rem;
      font-weight: 500;
      max-width: 600px;
    }
    
    @media (min-width: 768px) {
      .role {
        font-size: 1.5rem;
      }
    }

    .tagline {
      font-size: 1.1rem;
      color: var(--color-text);
      margin-bottom: 3rem; /* More space before actions */
      font-weight: 400;
      max-width: 580px;
      line-height: 1.6;
      opacity: 0.8;
    }

    .links {
      display: flex;
      flex-direction: column;
      gap: 2.5rem;
      align-items: flex-start;
    }

    .primary-actions {
      display: flex;
      gap: 1.5rem; /* Increased gap */
      flex-wrap: wrap;
    }

    .social-links {
      display: flex;
      gap: 2rem;
      align-items: center;
    }

    .btn-primary {
      background: var(--color-primary);
      color: #0f172a; /* Dark text for contrast */
      text-decoration: none;
      padding: 1rem 2rem;
      border-radius: var(--radius-lg);
      font-weight: 700;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      box-shadow: 0 4px 14px 0 rgba(var(--color-primary-rgb), 0.39);
      border: 2px solid var(--color-primary);
    }

    .btn-primary:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(var(--color-primary-rgb), 0.45);
      background: #22c55e;
      border-color: #22c55e;
    }

    .btn-secondary {
      background: transparent;
      color: var(--color-text);
      border: 2px solid var(--color-border);
      text-decoration: none;
      padding: 1rem 2rem;
      border-radius: var(--radius-lg);
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .btn-secondary:hover {
      border-color: var(--color-primary);
      color: var(--color-primary);
      transform: translateY(-2px);
    }

    .link-item {
      font-weight: 600;
      color: var(--color-text);
      position: relative;
      text-decoration: none;
    }

    .link-item::after {
      content: '';
      position: absolute;
      width: 100%;
      height: 2px;
      bottom: -4px;
      left: 0;
      background: var(--gradient-primary);
      transform: scaleX(0);
      transform-origin: right;
      transition: transform 0.3s ease;
    }

    .link-item:hover {
      color: var(--color-primary);
    }

    .link-item:hover::after {
      transform: scaleX(1);
      transform-origin: left;
    }
    
    /* macOS Terminal Styles */
    .hero-graphic {
      display: none;
      justify-content: center;
      perspective: 1000px;
    }

    @media (min-width: 900px) {
      .hero-graphic {
        display: flex;
      }
    }

    .terminal-window {
      background: #1e1e1e; /* Keep absolute dark for macOS feel */
      backdrop-filter: blur(16px);
      border: 1px solid var(--color-border);
      border-radius: var(--radius-lg);
      padding: 1.25rem;
      width: 100%;
      max-width: 520px;
      box-shadow: var(--shadow-lg);
      font-family: 'JetBrains Mono', 'Fira Code', 'Courier New', monospace;
      font-size: 0.85rem;
      line-height: 1.6;
      transition: transform 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
      transform-style: preserve-3d;
      transform: rotateY(-8deg) rotateX(4deg);
    }
    
    :global([data-theme="light"]) .terminal-window {
       background: #1e1e1e; /* Stay dark for realism */
       box-shadow: var(--shadow-lg);
    }

    .terminal-window:hover {
      transform: rotateY(0) rotateX(0) translateY(-10px);
    }

    .terminal-header {
      display: flex;
      gap: 0.5rem;
      margin-bottom: 1.5rem;
    }

    .dot {
      width: 12px;
      height: 12px;
      border-radius: 50%;
    }

    .red { background: #ff5f56; }
    .yellow { background: #ffbd2e; }
    .green { background: #27c93f; }

    .terminal-body {
      color: #cbd5e1;
      min-height: 260px; /* Prevent Layout Shift */
    }

    .line {
      min-height: 1.5em;
      display: flex;
      align-items: flex-start;
      margin-bottom: 0.15rem;
      white-space: pre;
    }

    /* Animation Styles - :global for dynamic injection */
    :global(.prompt) {
      color: #64748b;
      margin-right: 0.6rem;
      font-weight: bold;
    }

    :global(.cmd-text) {
      color: #f8fafc;
    }

    :global(.nest-tag) {
      color: var(--color-primary);
      font-weight: bold;
      margin-right: 0.5rem;
    }

    :global(.nest-msg) {
      color: var(--color-primary);
    }

    :global(.presentation-key) {
      color: var(--color-primary);
      margin-right: 0.5rem;
      font-weight: bold;
    }

    :global(.presentation-val) {
      color: #f8fafc;
    }

    .cursor {
      display: inline-block;
      width: 0.55em;
      height: 1.1em;
      background-color: var(--color-primary);
      margin-left: 2px;
      animation: blink 1.2s step-end infinite;
      vertical-align: middle;
      border-radius: 1px;
    }

    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }

    .decoration {
      position: absolute;
      top: -50px;
      right: -50px;
      width: 200px;
      height: 200px;
      background: var(--gradient-primary);
      filter: blur(80px);
      opacity: 0.2;
      border-radius: 50%;
      z-index: -1;
      pointer-events: none;
    }
  </style>

<script>
  function initTerminal() {
    const container = document.getElementById('console-content');
    if (!container) return;

    // --- Content ---
    const commandText = "npm run start:prod";
    
    const nestLogs = [
      '<span class="nest-tag">[Nest]</span> <span class="nest-msg">LOG [NestFactory] Starting Nest application...</span>',
      '<span class="nest-tag">[Nest]</span> <span class="nest-msg">LOG [InstanceLoader] AppModule dependencies initialized</span>',
      '<span class="nest-tag">[Nest]</span> <span class="nest-msg">LOG [NestApplication] Nest application successfully started</span>'
    ];

    const presentation = [
      '<span class="presentation-key">whoami:</span> <span class="presentation-val">Backend Developer · 4+ años de exp.</span>',
      '<span class="presentation-key">criterio:</span> <span class="presentation-val">Mantenibilidad · Performance</span>',
      '<span class="presentation-key">stack:</span> <span class="presentation-val">Node.js · NestJS · TypeScript</span>',
      '<span class="presentation-key">db:</span> <span class="presentation-val">PostgreSQL</span>'
    ];

    container.innerHTML = '';
    const wait = (ms) => new Promise(resolve => setTimeout(resolve, ms));
    
    const cursor = document.createElement('span');
    cursor.className = 'cursor';

    async function run() {
      await wait(800); 

      // PHASE 1: Command Typing
      const cmdLine = document.createElement('div');
      cmdLine.className = 'line';
      
      const promptSpan = document.createElement('span');
      promptSpan.className = 'prompt';
      promptSpan.textContent = '$';
      
      const cmdSpan = document.createElement('span');
      cmdSpan.className = 'cmd-text';
      
      cmdLine.appendChild(promptSpan);
      cmdLine.appendChild(document.createTextNode(' '));
      cmdLine.appendChild(cmdSpan);
      cmdLine.appendChild(cursor);
      container.appendChild(cmdLine);

      for (const char of commandText) {
        cmdSpan.textContent += char;
        cmdLine.appendChild(cursor); 
        await wait(30 + Math.random() * 50);
      }

      await wait(600); 

      // PHASE 2: NestJS Logs (Instant Stagger)
      for (const logHTML of nestLogs) {
        if (cursor.parentNode) cursor.parentNode.removeChild(cursor);
        
        const logLine = document.createElement('div');
        logLine.className = 'line';
        logLine.innerHTML = logHTML;
        
        container.appendChild(logLine);
        logLine.appendChild(cursor);
        
        await wait(180 + Math.random() * 120); 
      }

      await wait(800); 

      // PHASE 3: Presentation Text (Line Popups)
      for (const itemHTML of presentation) {
        if (cursor.parentNode) cursor.parentNode.removeChild(cursor);
        
        const itemLine = document.createElement('div');
        itemLine.className = 'line';
        itemLine.innerHTML = itemHTML;
        
        container.appendChild(itemLine);
        itemLine.appendChild(cursor);
        
        await wait(350 + Math.random() * 250); 
      }
    }

    run();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initTerminal);
  } else {
    initTerminal();
  }
</script>
